# GitHub网页上传更新包

这个包只包含新版需要新增或覆盖的文件。请保留原仓库中的照片及视频。

1. 下载并解压ZIP。
2. 打开 this-is-agui 仓库首页，选择 Add file → Upload files。
3. 将解压后看到的 assets 文件夹，以及 index.html、updates.css、interactions.js 一起拖入上传区。不要上传ZIP，也不要把最外层 browser-update 文件夹拖进去。
4. 等待文件上传完成，再点击 Commit changes。
5. 保持原GitHub Pages设置，等待Actions中的部署任务完成，再访问原网址并刷新。

上传预览中应出现 index.html 与 assets/yunnan-plan.pdf 这样的路径。不能出现 browser-update/index.html。

若网络不稳定，可以分两次提交：先上传assets文件夹，再上传三个网页文件。务必保留assets文件夹层级。

云南白药PDF已压缩为网页阅读版本，页数保持一致；原始大文件保留在此前完整包中。其他项目内容与上一版一致。

本增量包依赖原仓库中的旧素材，单独在空文件夹打开可能看不到原摄影及视频。需要完整本地预览时，将这些文件覆盖到上一版完整文件夹。
